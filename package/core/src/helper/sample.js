// @ts-check

/**
 * 平台无关的纯函数示例库（占位，替换为真实逻辑）。
 * @module helper/sample
 */

/**
 * 求两个数之和。
 * @param {number} a
 * @param {number} b
 * @returns {number} a 与 b 之和
 */
export function add(a, b) {
  return a + b;
}
